/**
 * Use la abreviatura de propiedad de objeto con literales de objeto para crear y 
 * devolver un objeto con y namepropiedades .agegender
 */

 const createPerson = (name, age, gender) => {
    return {
      name,
      age,
      gender
    };
  };