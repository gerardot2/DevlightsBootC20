/**
 * Reemplace las dos asignaciones con una asignación de desestructuración equivalente. 
 * Todavía debe asignar las variables today y tomorrow los valores de todayy tomorrowdesde el HIGH_TEMPERATURESobjeto.
 */

 const HIGH_TEMPERATURES = {
    yesterday: 75,
    today: 77,
    tomorrow: 80
  };
  
  const {today, tomorrow} = HIGH_TEMPERATURES;